{
  "name": "download-softwares-checker",
  "version": "1.0.0",
  "description": "Express API to check download links, detect software version changes, and download binaries into a shared folder.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "refresh": "node scripts/refresh-links.js"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "semver": "^7.3.8"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
